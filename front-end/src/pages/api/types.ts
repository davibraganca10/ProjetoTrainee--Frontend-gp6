export type Professor = {
    id: number,
    nome: string,
    departamento: string,
    createdAt: Date,
    updatedAt: Date,
  }

export type User = {
    id: number,
    nome: string,
    email: string,
    senha: string,
    departamento: string,
    curso: string,
    createdAt: Date,
    updatedAt: Date,
  }
  